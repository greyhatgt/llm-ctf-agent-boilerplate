Single-byte XOR challenge.

File: artifacts/ciphertext.hex (hex-encoded bytes).
Task: find the single-byte key used to XOR-encrypt the plaintext and recover the flag.

Flag format: flag{...}

Tip: try XORing with keys 0..255 and check which result matches the flag pattern.
